# Tasks

- [x] 1.1 Add `refresh_session` to `Provider` trait (core); VW impl moves from inherent method
- [x] 1.2 registry.rs = composition root; only file importing backend crates
- [x] 1.3 ops.rs `&dyn Provider` (7 signatures); main.rs via `registry::open`
- [x] 1.4 Update backends subcommand + test to list `vw`
- [x] 1.5 Gates: clippy -D warnings clean, cargo test --workspace green

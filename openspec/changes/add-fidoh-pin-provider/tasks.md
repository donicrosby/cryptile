# Tasks: add-fidoh-pin-provider

## 1. Lib: provider threading

- [x] 1.1 `fidoh_perform_assertion` gains `Option<PinProviderHandle>`
      param, threaded into the v2 `GetAssertionExchange` literal
- [x] 1.2 Internal `exchange()` builder accepts/carries the provider;
      `pin_uv_auth`/`pin_uv_auth_protocol` stay `None`
- [x] 1.3 Login call-site in the lib passes the provider through
      (signature ripple kept internal to the fidoh path)

## 2. Error mapping

- [x] 2.1 Map `PinRequired`/`PinNotSet`/`PinTooLong`/`IncorrectPin`-
      spent/`PinBlocked`/`PinAuthBlocked` → AUTH (3) with hints
- [x] 2.2 Map `PinProviderFailed` → TRANSPORT (4)
- [x] 2.3 Unit tests: every new variant asserted to its class (totality)

## 3. CLI wiring

- [x] 3.1 Interactive login builds provider from `rpassword` prompt
      ("YubiKey PIN: "); no-tty runs pass `None`
- [x] 3.2 Provider construction failure surfaces as provider-failed,
      never as a panic or a partial prompt

## 4. Pins

- [x] 4.1 fidoh deps → rev `435a774`, version `0.1.0-beta.1`
- [x] 4.2 Lockfile regenerated by build; featured graph resolves

## 5. Gates

- [x] 5.1 fmt + clippy `-D warnings` on all three feature configs
- [x] 5.2 Full test matrix: default / fidoh / webauthn,fidoh — all green
- [x] 5.3 `openspec validate add-fidoh-pin-provider --strict` valid
